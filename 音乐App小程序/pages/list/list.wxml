<!--pages/list/list.wxml-->
<view wx:for="{{list}}" class='wrap' wx:key='id' bindtap='tap' data-id='{{item.id}}'>
  <image src="{{item.al.picUrl}}"></image>
  <view class='introduce'>
    <view>{{item.name}}</view>
    <view>{{item.ar[0].name }}</view>
  </view>
</view>
